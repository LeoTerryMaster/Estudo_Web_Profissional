
function ligar() {
    var idade = new Number(40); // int
    var nome = "Leandro"; // string
    var casodo = false; // boolean
    alert(idade instanceof Number);
    alert(nome + " " + casodo);

}